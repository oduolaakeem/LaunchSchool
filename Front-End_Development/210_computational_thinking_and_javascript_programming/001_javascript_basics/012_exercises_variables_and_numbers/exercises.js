// Exercise 1

// Declare a variable named numerator and set it to 10

var numerator = 10;

// Exercise 2

// Declare a variable named denominator and set it to 2

var denominator = 2;

// Exercise 3

//Declare a variable named answer and set it to numerater divided denominator

var answer = numerator / denominator;

console.log(answer);

// Exercise 4

// Declare a variable incrementer and set it to 1

var incrementer = 1;

// Exercise 5

// Declare a variable named start and set it to incrementer

var start = incrementer;

// Exercise 6

// Declare variables named end and difference and leave them undefined

var end;
var difference;

// Exercise 7

// Increment the incrementer variable by 1 three times using shorthand self-addition

incrementer += 1;
incrementer += 1;
incrementer += 1;

// Exercise 8

// Increment the incrementer variable by 1 twice using increment shorthand

incrementer++;
incrementer++;

// Exercise 9

// Set the value of end to incrementer and the value of difference to end minus start. log
// the value of end, which should be 6. Log the value of difference, which should be 5.

end = incrementer;
difference = end - start;
console.log(end);
console.log(difference);

// Exercise 10

// Write the arithmetic statement that will set a variable answer to the result 11 plus 31 multiplied by 3
// log the result, it should be 126

answer = (11 + 31) * 3;
console.log(answer);
